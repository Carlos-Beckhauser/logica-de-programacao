const sum = 2 + 4;
const diff = 10 - 5;
const multiply = 10 * 2356;
const division = 40 / 5;
const power = Math.pow(5, 2);
const rest = 24 % 5;
const squareRoot = Math.sqrt(5)

console.log (sum, diff, multiply, division, power, rest, squareRoot);